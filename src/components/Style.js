import { createMuiTheme } from '@material-ui/core/styles';

export const customPalette = createMuiTheme({
    palette: {
        primary: {
            main: '#2B2743'
        },
        secondary: {
            main: '#E52F2D'
        },
        error: {
            main: '#A9A2C0'
        }
    },
});
